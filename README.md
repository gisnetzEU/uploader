# uploaderSimple

## Setup
Follow these steps to run the application:

- Execute the server:
    ``` shell
    nodemon server.js
    ```
- Open the follow url in the browser:
    * http://localhost:3000/
# uploader
